# Configuración de AWS (AWS Academy usa credenciales temporales)
AWS_REGION=us-east-1
AWS_PROFILE=default


# Suffix para nombres de recursos (opcional, se genera automáticamente si no se especifica)
# DEPLOYMENT_SUFFIX=myproject

# Email para suscripción SNS (opcional, se solicita durante execute)
# SNS_EMAIL=your-email@example.com
